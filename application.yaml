# example values

indexing-settings:
  sites:
    - url: https://www.lenta.ru/
      name: Лента.ру
    - url: https://skillbox.ru/
      name: Skillbox
    - url: https://www.playback.ru/
      name: PlayBack.Ru

server:
  port: 8080

spring:
  datasource:
    username: root
    password: Cfkk161205
    url: jdbc:mysql://localhost:3306/
      search_engine?useSSL=false
      &requireSSL=false
      &allowPublicKeyRetrieval=true&characterEncoding=utf8
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true